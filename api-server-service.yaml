# api-server-service.yaml
# A simple ClusterIP service to give the api-server a stable DNS name.

apiVersion: v1
kind: Service
metadata:
  name: api-server-service
spec:
  type: ClusterIP
  selector:
    app: api-server
  ports:
    - port: 80
      targetPort: 80
